# Simple Diary 開発者ドキュメント

**バージョン**: 1.0.0
**最終更新日**: 2026-02-01

## 目次

1. [アーキテクチャ概要](#アーキテクチャ概要)
2. [技術スタック](#技術スタック)
3. [開発環境のセットアップ](#開発環境のセットアップ)
4. [プロジェクト構造](#プロジェクト構造)
5. [開発ワークフロー](#開発ワークフロー)
6. [テスト戦略](#テスト戦略)
7. [コーディング規約](#コーディング規約)
8. [デプロイ手順](#デプロイ手順)
9. [トラブルシューティング](#トラブルシューティング)

---

## アーキテクチャ概要

### システム構成

```
┌─────────────────────────────────────────┐
│           Frontend (React)              │
│  http://localhost:5173                  │
│                                         │
│  - React Router                         │
│  - React Context (State Management)    │
│  - Tailwind CSS                         │
└──────────────┬──────────────────────────┘
               │ REST API
               │ (JSON over HTTP)
┌──────────────▼──────────────────────────┐
│        Backend (Express)                │
│    http://localhost:3000                │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │     Controller Layer            │   │
│  │  (HTTP Request/Response)        │   │
│  └──────────┬──────────────────────┘   │
│             │                           │
│  ┌──────────▼──────────────────────┐   │
│  │     Service Layer               │   │
│  │  (Business Logic)               │   │
│  └──────────┬──────────────────────┘   │
│             │                           │
│  ┌──────────▼──────────────────────┐   │
│  │    Repository Layer             │   │
│  │  (Data Access)                  │   │
│  └──────────┬──────────────────────┘   │
│             │                           │
│  ┌──────────▼──────────────────────┐   │
│  │      SQLite Database            │   │
│  │   (./data/diary.db)             │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### 3層アーキテクチャ

#### 1. Controller層
- **役割**: HTTPリクエスト/レスポンスの処理
- **責務**:
  - リクエストパラメータの抽出
  - レスポンス形式の整形
  - HTTPステータスコードの設定

#### 2. Service層
- **役割**: ビジネスロジックの実装
- **責務**:
  - データの検証
  - ビジネスルールの適用
  - トランザクション管理

#### 3. Repository層
- **役割**: データアクセス
- **責務**:
  - SQL実行
  - データベース操作
  - データ変換

---

## 技術スタック

### バックエンド

| 技術 | バージョン | 用途 |
|------|-----------|------|
| Node.js | 20+ | ランタイム |
| Express | 4.18 | Webフレームワーク |
| TypeScript | 5.3 | 型安全性 |
| better-sqlite3 | 12.6 | SQLiteドライバ |
| Zod | 3.22 | バリデーション |
| Jest | 29.7 | テストフレームワーク |
| Supertest | 6.3 | API統合テスト |

### フロントエンド

| 技術 | バージョン | 用途 |
|------|-----------|------|
| React | 18.2 | UIライブラリ |
| Vite | 5.0 | ビルドツール |
| TypeScript | 5.2 | 型安全性 |
| React Router DOM | 6.21 | ルーティング |
| Tailwind CSS | 3.4 | スタイリング |
| Zod | 3.22 | バリデーション |
| Vitest | 1.1 | テストフレームワーク |
| Playwright | 1.40 | E2Eテスト |

---

## 開発環境のセットアップ

### 前提条件

- Node.js 20.x以上
- npm 10.x以上
- Git

### 初回セットアップ

```bash
# リポジトリのクローン
git clone <repository-url>
cd simple-diary

# バックエンドのセットアップ
cd backend
npm install
cp .env.example .env

# フロントエンドのセットアップ
cd ../frontend
npm install

# バックエンドの起動（別ターミナル）
cd backend
npm run dev

# フロントエンドの起動（別ターミナル）
cd frontend
npm run dev
```

### 環境変数

#### backend/.env

```env
PORT=3000
NODE_ENV=development
DATABASE_PATH=./data/diary.db
```

---

## プロジェクト構造

```
simple-diary/
├── backend/                    # バックエンド
│   ├── src/
│   │   ├── controllers/        # コントローラー層
│   │   │   └── EntryController.ts
│   │   ├── services/           # サービス層
│   │   │   └── EntryService.ts
│   │   ├── repositories/       # リポジトリ層
│   │   │   └── EntryRepository.ts
│   │   ├── models/             # 型定義
│   │   │   └── Entry.ts
│   │   ├── middlewares/        # ミドルウェア
│   │   │   ├── errorHandler.ts
│   │   │   └── validateRequest.ts
│   │   ├── routes/             # ルーティング
│   │   │   └── entryRoutes.ts
│   │   ├── database.ts         # データベース初期化
│   │   └── app.ts              # Expressアプリ
│   ├── tests/                  # テスト
│   │   ├── controllers/
│   │   ├── services/
│   │   ├── repositories/
│   │   ├── middlewares/
│   │   └── integration/
│   ├── data/                   # データベースファイル
│   ├── dist/                   # ビルド出力
│   ├── package.json
│   ├── tsconfig.json
│   ├── jest.config.js
│   └── .env
├── frontend/                   # フロントエンド
│   ├── src/
│   │   ├── components/         # UIコンポーネント
│   │   │   ├── EntryList.tsx
│   │   │   ├── EntryForm.tsx
│   │   │   └── SearchBar.tsx
│   │   ├── pages/              # ページコンポーネント
│   │   │   ├── EntryListPage.tsx
│   │   │   ├── EntryCreatePage.tsx
│   │   │   ├── EntryDetailPage.tsx
│   │   │   └── EntryEditPage.tsx
│   │   ├── contexts/           # React Context
│   │   │   ├── EntryContext.tsx
│   │   │   └── entryReducer.ts
│   │   ├── services/           # APIクライアント
│   │   │   └── api.ts
│   │   ├── types/              # 型定義
│   │   │   └── Entry.ts
│   │   ├── App.tsx             # ルートコンポーネント
│   │   ├── main.tsx            # エントリーポイント
│   │   └── index.css           # グローバルCSS
│   ├── tests/                  # テスト
│   │   ├── components/
│   │   ├── pages/
│   │   ├── contexts/
│   │   ├── services/
│   │   └── e2e/
│   ├── dist/                   # ビルド出力
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   ├── vitest.config.ts
│   ├── playwright.config.ts
│   └── tailwind.config.js
├── doc/                        # ドキュメント
│   ├── REQUIREMENTS.md         # 要件定義書
│   ├── DESIGN.md               # 設計書
│   ├── API.md                  # API仕様書
│   ├── USER_MANUAL.md          # ユーザーマニュアル
│   ├── DEVELOPMENT.md          # 開発者ドキュメント (本ファイル)
│   ├── PROGRESS_REPORT.md      # 進捗レポート
│   └── CODEX_REVIEW_PHASE3.md  # コードレビュー結果
├── CLAUDE.md                   # プロジェクト管理
└── README.md                   # プロジェクト概要
```

---

## 開発ワークフロー

### TDD (テスト駆動開発)

このプロジェクトはTDDアプローチで開発されています。

#### TDDサイクル

1. **RED**: テストを書く（失敗することを確認）
2. **GREEN**: テストをパスする最小限のコードを書く
3. **REFACTOR**: コードをリファクタリング

#### 実践例

```typescript
// 1. RED: テストを書く
describe('EntryRepository', () => {
  it('should create a new entry', () => {
    const entry = repository.create({
      date: '2026-01-31',
      content: 'Test content',
    });
    expect(entry).toHaveProperty('id');
  });
});

// 2. GREEN: 最小限の実装
class EntryRepository {
  create(data: CreateEntryRequest): Entry {
    // 最小限の実装
    return { id: 1, ...data, created_at: new Date().toISOString(), updated_at: new Date().toISOString() };
  }
}

// 3. REFACTOR: リファクタリング
class EntryRepository {
  create(data: CreateEntryRequest): Entry {
    // より良い実装に改善
    const now = new Date().toISOString();
    const result = this.db.prepare(
      'INSERT INTO entries (date, content, created_at, updated_at) VALUES (?, ?, ?, ?)'
    ).run(data.date, data.content, now, now);
    return this.findById(result.lastInsertRowid as number)!;
  }
}
```

### Git ワークフロー

```bash
# 新機能の開発
git checkout -b feature/new-feature
# 開発...
git add .
git commit -m "Add new feature"
git push origin feature/new-feature
# Pull Request作成
```

---

## テスト戦略

### テストピラミッド

```
         /\
        /  \         E2E Tests (25)
       /────\        - Playwright
      /      \       - User flows
     /────────\
    /          \     Integration Tests (included in 155)
   /────────────\    - Supertest
  /              \   - API endpoints
 /────────────────\
/                  \ Unit Tests (244)
──────────────────── - Jest / Vitest
                     - Components, Services, Repositories
```

### テストカバレッジ目標

- **バックエンド**: 80%以上（達成: 100%）
- **フロントエンド**: 80%以上（達成: 98.29%）
- **E2E**: 主要ユーザーフロー全て（達成: 100%）

### テスト実行

```bash
# バックエンド
cd backend
npm test                    # 全テスト
npm run test:coverage       # カバレッジ付き
npm run test:watch          # ウォッチモード

# フロントエンド
cd frontend
npm test                    # 全テスト
npm run test:coverage       # カバレッジ付き
npm run e2e                 # E2Eテスト
npm run e2e:ui              # E2EテストUI
```

### テスト種別

#### 1. ユニットテスト
- **対象**: 個別の関数・クラス・コンポーネント
- **ツール**: Jest (backend), Vitest (frontend)
- **例**: EntryRepository.test.ts

#### 2. 統合テスト
- **対象**: APIエンドポイント
- **ツール**: Supertest
- **例**: entries.test.ts (integration)

#### 3. E2Eテスト
- **対象**: ユーザーフロー
- **ツール**: Playwright
- **例**: entries.spec.ts

---

## コーディング規約

### TypeScript

- **strict mode**: 有効
- **型注釈**: 関数の戻り値は明示的に型を指定
- **any**: 可能な限り使用しない

### 命名規則

- **ファイル名**: PascalCase（コンポーネント）、camelCase（その他）
- **変数**: camelCase
- **定数**: UPPER_SNAKE_CASE
- **型・インターフェース**: PascalCase
- **コンポーネント**: PascalCase

### コードフォーマット

- **Prettier**: 自動フォーマット
- **ESLint**: リント

```bash
# フォーマット
npm run format

# リントチェック
npm run lint
```

### コメント

- **TSDoc**: 関数・クラスには必ず記載
- **インラインコメント**: 複雑なロジックのみ

```typescript
/**
 * Creates a new entry
 * @param request The entry data to create
 * @returns Array containing the created entry
 */
createEntry(request: CreateEntryRequest): Entry[] {
  // Implementation
}
```

---

## デプロイ手順

### ローカル環境（開発用）

```bash
# バックエンド
cd backend
npm run dev

# フロントエンド
cd frontend
npm run dev
```

### 本番ビルド

```bash
# バックエンド
cd backend
npm run build
npm start

# フロントエンド
cd frontend
npm run build
npm run preview
```

### 本番環境への配置

1. **バックエンド**
   ```bash
   cd backend
   npm run build
   # dist/ フォルダを本番サーバーにコピー
   ```

2. **フロントエンド**
   ```bash
   cd frontend
   npm run build
   # dist/ フォルダを静的ホスティングサービスにデプロイ
   ```

3. **環境変数**
   - 本番環境用の `.env` ファイルを作成
   - `NODE_ENV=production` を設定

---

## トラブルシューティング

### ビルドエラー

#### TypeScriptエラー

```bash
# 型チェック
npx tsc --noEmit

# エラー詳細表示
npx tsc --noEmit --pretty
```

#### 依存関係エラー

```bash
# node_modules削除
rm -rf node_modules
rm package-lock.json

# 再インストール
npm install
```

### テストエラー

#### データベースエラー

```bash
# テスト用DBを削除
rm backend/data/diary.db

# テスト再実行
npm test
```

#### E2Eテストエラー

```bash
# Playwrightインストール
npx playwright install

# E2E再実行
npm run e2e
```

### 実行時エラー

#### ポートが使用中

```bash
# ポートを使用しているプロセスを確認
lsof -ti:3000
lsof -ti:5173

# プロセスを終了
kill -9 <PID>
```

#### データベースロック

```bash
# SQLiteロック解除
rm backend/data/diary.db-journal
```

---

## パフォーマンス最適化

### バックエンド

- ✅ データベースインデックス設定済み（date列）
- ✅ プリペアドステートメント使用
- ✅ 適切なHTTPステータスコード

### フロントエンド

- ✅ Reactコンポーネントのメモ化（React.memo, useCallback, useMemo）
- ✅ 遅延ローディング（React.lazy）
- ✅ Tailwind CSSの最適化

---

## セキュリティ

### 対策済み

- ✅ **SQLインジェクション**: プリペアドステートメント
- ✅ **XSS**: React標準のエスケープ + Zodバリデーション
- ✅ **CSRF**: （ローカル環境のため対象外）

### 今後の対策

- ⏳ 認証・認可
- ⏳ レート制限
- ⏳ HTTPS化

---

## 開発ツール

### 推奨VSCode拡張機能

- **ESLint**: コードリント
- **Prettier**: コードフォーマット
- **TypeScript**: 型チェック
- **Tailwind CSS IntelliSense**: CSS補完
- **Jest**: テスト実行

### デバッグ

#### バックエンド
```bash
# デバッグモード
npm run dev
# Chrome DevTools で node --inspect
```

#### フロントエンド
- React DevTools (ブラウザ拡張)
- ブラウザのデベロッパーツール

---

## コントリビューション

### Pull Request

1. Issue作成
2. ブランチ作成（`feature/xxx`, `fix/xxx`）
3. 開発 + テスト
4. Lintチェック + フォーマット
5. Pull Request作成
6. レビュー + マージ

### コードレビュー

- ✅ テストカバレッジ80%以上
- ✅ Lint/Formatエラーなし
- ✅ TypeScriptエラーなし
- ✅ 全テスト成功

---

## リソース

- [React ドキュメント](https://react.dev/)
- [Express ドキュメント](https://expressjs.com/)
- [TypeScript ドキュメント](https://www.typescriptlang.org/)
- [Tailwind CSS ドキュメント](https://tailwindcss.com/)
- [Playwright ドキュメント](https://playwright.dev/)

---

**Happy Coding! 🚀**
